server {
    listen 80;
    server_name dev.ohmyrevit.pp.ua;

    # === ВИРІШЕННЯ ПРОБЛЕМИ: Збільшено максимальний розмір файлу для завантаження ===
    client_max_body_size 100m; # Дозволяє завантажувати файли до 100MB

    # Збільшені буфери для кращої продуктивності
    proxy_buffer_size   128k;
    proxy_buffers   4 256k;
    proxy_busy_buffers_size   256k;

    location /uploads/ {
        alias /app/uploads/;
        expires 7d; # Кешування на 7 днів
        add_header Cache-Control "public";
    }

    # Маршрут для API
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Маршрут для всього іншого (Frontend)
    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # === ДОДАНО: Правильна обробка WebSocket для Hot Reload ===
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        # ==========================================================
    }
}